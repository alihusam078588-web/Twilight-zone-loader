name: Twisted of the Day (Auto Wiki)

on:
  schedule:
    - cron: '0 22 * * *' # Runs daily at 22:00 UTC (≈ 1:00 AM Iraq time)
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest

    steps:
      - name: Fetch Twisted from Wiki and send webhook
        run: |
          python3 << 'EOF'
          import requests
          import re
          from datetime import datetime

          WIKI_URL = "https://dandys-world-robloxhorror.fandom.com/wiki/Daily_Twisted_Board"
          WEBHOOK_URL = "https://discord.com/api/webhooks/1449788203082191100/E6NMZ-b7wc-PHLS4lRY0E6ZV4Yas5IXLLHOIP8iN-8fn5L1KNkcBUhWsQytL2T9BVLpK"

          response = requests.get(WIKI_URL, timeout=15)
          html = response.text

          match = re.search(
              r"Currently, the board is occupied by\s*([A-Za-z\s]+)\.",
              html
          )

          twisted = match.group(1).strip() if match else "Unknown Twisted"

          payload = {
              "username": "Dandy's World",
              "embeds": [
                  {
                      "title": "🌀 Twisted of the Day",
                      "description": f"**{twisted}**",
                      "color": 10181046,
                      "footer": {
                          "text": "Daily Twisted Board"
                      },
                      "timestamp": datetime.utcnow().isoformat()
                  }
              ]
          }

          requests.post(WEBHOOK_URL, json=payload)
          EOF