name: Twisted of the Day

on:
  schedule:
    - cron: '0 22 * * *' # 22:00 UTC ≈ 1:00 AM Iraq time
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest

    steps:
      - name: Detect Today's Twisted and send webhook
        run: |
          python3 << 'EOF'
          import requests
          import xml.etree.ElementTree as ET
          import re
          from datetime import datetime

          RSS_URL = "https://twisted-of-the-day.tumblr.com/rss"
          WEBHOOK_URL = "https://discord.com/api/webhooks/1449788203082191100/E6NMZ-b7wc-PHLS4lRY0E6ZV4Yas5IXLLHOIP8iN-8fn5L1KNkcBUhWsQytL2T9BVLpK"

          rss = requests.get(RSS_URL, timeout=15).text
          root = ET.fromstring(rss)

          twisted = "Unknown Twisted"

          for item in root.findall(".//item"):
              desc = item.find("description")
              if desc is None or not desc.text:
                  continue

              text = desc.text

              # Look for: Today's twisted is… NAME
              match = re.search(
                  r"Today.?s twisted is…?\s*<[^>]*>\s*([^<]+)",
                  text,
                  re.IGNORECASE
              )

              if match:
                  twisted = match.group(1).strip()
                  break

          payload = {
              "username": "Dandy's World",
              "embeds": [
                  {
                      "title": "🌀 Twisted of the Day",
                      "description": f"**{twisted}**",
                      "color": 10181046,
                      "footer": {
                          "text": "Twisted of the Day"
                      },
                      "timestamp": datetime.utcnow().isoformat()
                  }
              ]
          }

          requests.post(WEBHOOK_URL, json=payload)
          EOF